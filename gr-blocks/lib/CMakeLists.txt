# Copyright 2011,2012,2016,2018,2019 Free Software Foundation, Inc.
#
# This file was generated by gr_modtool, a tool from the GNU Radio framework
# This file is a part of gr-openemissions
#
# SPDX-License-Identifier: GPL-3.0-or-later
#

########################################################################
# Find PiGPIO
########################################################################
find_package(pigpio REQUIRED)

########################################################################
# Setup library
########################################################################
include(GrPlatform) #define LIB_SUFFIX

list(APPEND openemissions_sources
    pad_crop_impl.cc
    pigpio_sink_impl.cc
    pigpiod.cc
    histogram_impl.cc
    accumulate_impl.cc
)

set(openemissions_sources "${openemissions_sources}" PARENT_SCOPE)
if(NOT openemissions_sources)
    MESSAGE(STATUS "No C++ sources... skipping lib/")
    return()
endif(NOT openemissions_sources)

add_library(gnuradio-openemissions SHARED ${openemissions_sources})
target_link_libraries(gnuradio-openemissions gnuradio::gnuradio-runtime pigpio::pigpiod_if2)
target_include_directories(gnuradio-openemissions
    PUBLIC $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../include>
    PUBLIC $<INSTALL_INTERFACE:include>
  )
set_target_properties(gnuradio-openemissions PROPERTIES DEFINE_SYMBOL "gnuradio_openemissions_EXPORTS")

if(APPLE)
    set_target_properties(gnuradio-openemissions PROPERTIES
        INSTALL_NAME_DIR "${CMAKE_INSTALL_PREFIX}/lib"
    )
endif(APPLE)

########################################################################
# Install built library files
########################################################################
include(GrMiscUtils)
GR_LIBRARY_FOO(gnuradio-openemissions)

########################################################################
# Print summary
########################################################################
message(STATUS "Using install prefix: ${CMAKE_INSTALL_PREFIX}")
message(STATUS "Building for version: ${VERSION} / ${LIBVER}")

########################################################################
# Build and register unit test
########################################################################
include(GrTest)

# If your unit tests require special include paths, add them here
#include_directories()
# List all files that contain Boost.UTF unit tests here
list(APPEND test_openemissions_sources
)
# Anything we need to link to for the unit tests go here
list(APPEND GR_TEST_TARGET_DEPS gnuradio-openemissions)

if(NOT test_openemissions_sources)
    MESSAGE(STATUS "No C++ unit tests... skipping")
    return()
endif(NOT test_openemissions_sources)

foreach(qa_file ${test_openemissions_sources})
    GR_ADD_CPP_TEST("openemissions_${qa_file}"
        ${CMAKE_CURRENT_SOURCE_DIR}/${qa_file}
    )
endforeach(qa_file)
