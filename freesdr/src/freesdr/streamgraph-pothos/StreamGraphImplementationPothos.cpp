#include "BlockImplementationPothos.hpp"

using namespace freesdr;
using namespace Pothos;

/*
BlockImplementation::BlockImplementation(Block & block, std::initializer_list<SampleType> inputStreams, std::initializer_list<SampleType> outputStreams)
{
  for (auto inputStream & : inputStreams)
  {
    setupInput(inputStream.index(), inputStream.type());
  }
  for (auto outputStream & : outputStreams)
  {
    setupOutput(outputStream.index(), outputStream.type());
  }
}

void BlockImplementation::work()
{
  Blo
}
*/
